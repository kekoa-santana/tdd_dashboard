"""
Lightweight computation library for the TDD Dashboard.

These modules are snapshots from the player_profiles projection engine.
They contain only the functions needed for live dashboard computation
(matchup scoring, game K simulation, zone charts).

To update: copy the corresponding file from player_profiles/src/ and
adjust imports from `src.` to `lib.`.
"""
